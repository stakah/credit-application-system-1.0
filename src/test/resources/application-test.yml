spring:
  datasource:
    url: jdbc:h2:mem:test_db
    username: cami
    password:
  jpa:
    show-sql: true
    #hibernate:
    properties:
      hibernate.format_sql: true
  flyway:
    enabled: false
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false
        web-allow-others: false
springdoc:
  swagger-ui:
    path: /swagger-ui.html